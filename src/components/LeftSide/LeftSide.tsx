import React, { useEffect } from "react"
import * as styles from "./LeftSideStyles.module.css"

interface IProps {
}

export const LeftSide = (props: IProps) => {

    return (
        <div className={styles['kunteynir']}>
        LeftSide
        </div>
    )
}
